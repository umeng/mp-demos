// app.js
require('umtrack-wx');

App({
  umengConfig: {
    appKey: 'YOUR_APP_KEY',
    useOpenid: false,
    autoGetOpenid: false,
    debug: true
  },

  onLaunch(options) {
  },

  onShow(options) {
  },

  onHide() {
  },

  onError(/* msg */) {
  },
  
  globalData: {
  }
});
